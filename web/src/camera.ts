export function cameraError(error:unknown){const e=error as {name?:string};switch(e.name){case 'NotAllowedError':return 'Camera permission was denied. Allow camera access in your browser, then try again.';case 'NotFoundError':return 'No camera was found. Connect a webcam, then try again.';case 'NotReadableError':return 'Your camera is busy or unavailable. Close other camera apps, then try again.';case 'OverconstrainedError':return 'That camera is unavailable. Choose another camera.';default:return 'Camera could not start. Check your webcam and browser permissions, then try again.';}}
export function stopStream(stream:MediaStream|null){stream?.getTracks().forEach(track=>track.stop());}
